<div class="container">  
  <h1>Favorite List</h1>
  <div class="content">
      <div class="row">

        <% @favorite_photos.each do |favorite_photo| %>
          <div class="col-md-6">
            <div class="post">
              <div class="photo">
                <p><%= image_tag favorite_photo.image.url %></p>
              </div>
              <div class="favorite_content">
                <p class="user_name">
                  <%= favorite_photo.user.name %>
                </p>
                <p class = "photo_caption">
                  <%= favorite_photo.caption %>
                </p>
                <p class="date">
                  <%= favorite_photo.created_at.strftime("%Y-%m-%d %H:%M ") %>
                </p>
                <p>
                  
                  <%= link_to favorite_photo.favorite_with(current_user), method: :delete do %>
                    <span>お気に入り</span><i class="fas fa-heart fa-lg heart"></i>
                  <% end%>
                </p>

              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <br>

  <button type="button" class="btn btn-default"><%= link_to "Back to Profile", user_path(current_user.id) %></button>
  
  </div>
</div>